name: Continuous integration

on:
  push:
    branches: [ master, develop ]
  pull_request:
    branches: [ develop ]

jobs:
  ci:
    runs-on: ${{ matrix.operating-system }}

    strategy:
      fail-fast: false
      matrix:
        operating-system: [ ubuntu-latest ]
        php-version: [ '7.4', '8.0', '8.1' ]
        include:
          - php-version: '7.4'
            coverage: true

    steps:
      - name: CI
        uses: oat-sa/tao-extension-ci-action@714be4a6d6ca87e87e97138cff164497abd3697b
        with:
          php: ${{ matrix.php-version }}
          coverage: ${{ matrix.coverage }}
