name: Continuous integration

on:
  push:
    branches: [ master, develop ]
  pull_request:
    branches: [ develop ]

jobs:
  ci:
    runs-on: ${{ matrix.operating-system }}

    strategy:
      fail-fast: false
      matrix:
        operating-system: [ ubuntu-latest ]
        php-version: [ '7.4', '8.0', '8.1' ]
        include:
          - php-version: '7.4'
            coverage: true

    steps:
      - name: CI
        uses: oat-sa/tao-extension-ci-action@0a72906cc9bc362539d9717d1e386bf9072b5a49
        with:
          php: ${{ matrix.php-version }}
          coverage: ${{ matrix.coverage }}
